;; Early init file is for configuration before the frame's GUI and
;; package.el are initialized.

;; Disable the menu-bar, tool-bar, scroll bars, and window fringes
(menu-bar-mode -1)
(tool-bar-mode -1)
(scroll-bar-mode -1)
(set-fringe-mode 0)

;; (add-to-list 'default-frame-alist '(alpha . (85 . 85)))
